import 'package:flutter/material.dart';

class AppBorders{
  static const biggestCorner = BorderRadius.all(Radius.circular(70));
  static const bigCorner = BorderRadius.all(Radius.circular(60));
  static const mediumCorner = BorderRadius.all(Radius.circular(20));
  static const smallCorner = BorderRadius.all(Radius.circular(14));
}